export const cellsSelector = (store) => store.cells;

export const winPatternSelector = (store) => store.winPattern;

export const gameStatusSelector = (store) => store.gameStatus;

export const currentPlayerSelector = (store) => store.currentPlayer;
